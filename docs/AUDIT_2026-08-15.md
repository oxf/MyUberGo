# Project audit — 2026-08-15

Ad-hoc audit of the working tree as of 2026-08-15: the uncommitted `services/location-service` (Slice 1 of 4) change set and its doc sync across `CLAUDE.md`/`README.md`/`PLAN.md`/`docs/location/LOCATION_SPEC.md`, plus pre-existing cross-service architecture issues surfaced while reading the diff. This is a flagging pass, not a fix pass — nothing outside this file was changed. `fix` items are simple corrections; `needs-decision` items are real tradeoffs for the repo owner to resolve; `no action` items are recorded so they aren't rediscovered.

Severity: 🔴 blocks correctness or commit · 🟠 real defect, low cost to fix · 🟡 needs a decision or is a minor/cosmetic gap · ⚪ informational.

| # | Sev | Area | Finding | Location | Verdict |
|---|---|---|---|---|---|
| 1 | 🔴 | Commit hazard | `services/e2e-test/cmd/main.go` calls `actors.RunLocationRadiusScenario`, defined only in the untracked `scenario_location_radius.go`. Committing the currently-staged set alone breaks the `e2e-test` build. | `services/e2e-test/cmd/main.go:186` | fix |
| 2 | 🟠 | Commit hazard / doc drift | `.env.example` is untracked, but `PLAN.md` records it as an existing secret-hygiene deliverable — doesn't exist for a fresh clone. | `PLAN.md:372` | fix |
| 3 | 🟠 | location-service Slice 1 | No over-fetch on the first broadcast round: `limit = attempt × PoolWidthPerAttempt` = 5 on attempt 1, identical to `BroadcastSize`. Spec calls for a 5× cushion precisely because Location returns offline drivers too; intersecting against `drivers:online` can starve the round to 1 candidate instead of the 5 the rating-only fallback guarantees. No test asserts candidate *count*, only mechanism. | `matching-service/internal/application/command/broadcast_offers.go:213`; spec §11:438, §2.3:100 | fix |
| 4 | 🟡 | location-service Slice 1 | No `LocationIngestor` port; handler calls the CQRS command directly. Functionally equivalent seam today; the port would earn its keep once Slice 2's WebSocket adapter exists as a second caller. | spec §2.7, §13:481/496 | needs-decision |
| 5 | 🟡 | location-service Slice 1 | Read path doesn't filter by `loc:drivers:lastseen`; spec asks for this specifically so a `StalenessWorker` outage degrades rather than silently serves stale positions. | `location-service/internal/infrastructure/cache/driver_location_redis_repository.go:116-143`; spec §5.3:232 | fix |
| 6 | 🟡 | location-service Slice 1 | §12's two metrics (geo-index-size-vs-`drivers:online` gauge, nearby-candidates-returned count) are unimplemented. The candidates-returned metric would have made finding #3 visible in production. | spec §12 | fix |
| 7 | ⚪ | location-service Slice 1 | Dockerfile copies `./cmd`+`./internal` explicitly rather than "the whole dir" per spec wording; behaviorally this avoids the exact stale-copy bug the spec was warning against. | `services/location-service/Dockerfile:18-19` | no action |
| 8 | ⚪ | Doc drift | `LOCATION_SPEC.md` still says "Slice 1 in progress" with all 11 §13 boxes unchecked, while `PLAN.md` records the same work done and live-verified. | `docs/location/LOCATION_SPEC.md:7`; `PLAN.md:433-451` | fix |
| 9 | 🟡 | location-service data lifetime | TTL asymmetry: `loc:driver:{id}` expires in 5 min; `loc:drivers:geo`/`lastseen` rely solely on the 30s-cadence `StalenessWorker`. After a >5-min gap, `LastPosition` returns nil, silently disabling both the teleport guard and the stale-ordering guard for the driver's next ping. | `location-service/internal/application/command/ingest_pings.go:83`; `internal/domain/ping.go:69` | needs-decision |
| 10 | 🟠 | location-service validation | `AccuracyM > MaxAccuracyM` is false for `NaN` and for negative values, so both are accepted; `SpeedMps`/`HeadingDeg` are stored with no validation at all. Coordinates already get a proper NaN guard — accuracy doesn't. | `location-service/internal/domain/ping.go:59` | fix |
| 11 | 🔴 | Cross-service architecture | matching-service marks a ride `"failed"` in Redis after 5 exhausted retry attempts; ride-service's Postgres CHECK constraint has no such status, and no topic/consumer carries the fact. A ride nobody could be matched stays `'Requested'` in the system of record forever — invisible to the client's poll, the admin dashboard, and every metric. | `matching-service/internal/domain/ride.go:8`; `services/shared/migrations/sql/0003_ride.up.sql:23` | needs-decision |
| 12 | 🟠 | Cross-service architecture | `driver.shift` has no `status` column; `domain.Shift.Status` is set in memory but never persisted. matching-service stores the *shift* status value (`"Online"`/`"Ended"`) into a Redis hash field named `"status"` that sits conceptually beside driver-status values (`Offline`/`Online`/`OnRide"`) — `"OnRide"` can never appear there. Confirm intentional (shift start/end as a signal, not a queryable state) and document if so. | `driver-service/internal/persistence/shift_postgres_repository.go:43-47`; `matching-service/internal/infrastructure/cache/driver_shift_redis_repository.go:28` | needs-decision |
| 13 | 🟡 | Cross-service architecture | Status casing forks on the wire for the same ride state: `"Matched"`/`"matched"`, `"Cancelled"`/`"cancelled"` between ride-service and matching-service responses; the e2e simulator already asserts both spellings in different files. Low cost to align only if touching either contract for other reasons. | `contracts/http/ride-service.go` (`RideDto.Status`) vs `contracts/http/matching-service.go` (`AcceptRideResponse.Status`) | no action |
| 14 | 🟡 | Cross-service architecture | `billing.ledger_account.owner_id` is polymorphic (`auth.client(id)` or `driver.driver(id)`, disambiguated only by the sibling `type` column). Every read site checked resolves type-first correctly; flagged as a trap for a future query that doesn't. | `billing-service/internal/application/command/create_invoice_from_ride.go:138,167` | no action |
| 15 | ⚪ | Cross-service architecture | `ride.started` and `payment.failed` topics have zero consumers repo-wide. Confirm forward-looking-by-design vs. dead. | `ride-service/internal/application/command/start_ride.go:84`; `billing-service/internal/application/command/finalize_charge_failed.go:141` | needs-decision |
| 16 | ⚪ | Cross-service architecture | Outbox `EventType` strings are inconsistent (`"ShiftUpdatedEvent"` vs. `"RideRequested"` etc.); matching's direct `ride.accepted` publish sets none. Cosmetic, no functional impact found. | `driver-service/internal/application/command/update_shift.go:134` vs `ride-service/internal/application/command/create_ride.go:161` | no action |
| 17 | ⚪ | Cross-service architecture | `auth.Client.Rating`/`TotalRidesCompleted` duplicate `driver.Driver`'s fields and are never populated by any code path. Dead fields. | `auth-service/internal/domain/client.go:6-7` | fix (trivial) |
| 18 | 🟠 | Doc drift | `README.md` still marks the API Gateway "Scaffolded, not implemented"; Kong has been the sole ingress for weeks. | `README.md:109` | fix |
| 19 | 🟡 | Doc drift | `README.md` Phase 1 roadmap boxes for auth-service and billing-service are unchecked despite `PLAN.md` recording both shipped (2026-07-25, 2026-08-01). | `README.md:176-177,185` | fix |
| 20 | 🟡 | Doc drift | `PLAN.md` claims testcontainers/consumer-test-harness work for driver-service and matching-service is "not started"; both exist and are already committed. | `PLAN.md:394,425`; `driver-service/internal/consumers/testhelpers_test.go`; `matching-service/internal/consumers/testhelpers_test.go` | fix |
