meta {
  name: Log in
  type: http
  seq: 3
}

post {
  url: http://localhost:8000/login
  body: json
  auth: inherit
}

body:json {
  {
    "Email": "{{email}}",
    "Password": "Password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
