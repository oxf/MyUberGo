meta {
  name: Refresh token
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "Refresh_Token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
